<div class="gridArticle">
	<h1 class="topicTitle gridSpan12">{{ entry.title }}</h1>
	{% if entry.fSeoDescription %}
		<p class="gridSpan12 topicLead">{{ entry.fPlainTextI18nMultiline }}</p>
	{% endif %}
</div>