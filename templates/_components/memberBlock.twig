{% set articleBedtime = entry.postDate|date_modify('+3 months') %}
{% set isPastBedtime = now > articleBedtime %}

{% set user = craft.app.user.identity %}
{% set isGuest = craft.app.user.isGuest %}
{% set hasGroups = user and user.getGroups()|length > 0 %}

{% if now == articleBedtime %}
    {% redirect 'login' %}
{% endif %}

Artikel für Nichtmitglieder noch {{ date(articleBedtime|date('c')).diff(now).days }}  Tage verfügbar.