{% set accessMessage %}
    {% if not isSunset %}
        Artikel für Nichtmitglieder noch {{ daysLeft }} Tage verfügbar.
        <a href="https://www.espacesuisse.ch/de/verband/mitglieder">Mitglied werden</a>
    {% else %}
        Artikel nur für Mitglieder verfügbar. Bitte
        <a href="{{ url('member-login') }}">Loggen Sie sich ein.</a>
        oder
        <a href="https://www.espacesuisse.ch/de/verband/mitglieder">Mitglied werden</a>.
    {% endif %}
{% endset %}

<p class="gridStart2 gridSpan9">{{ accessMessage|raw }}</p>