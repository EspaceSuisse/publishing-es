{% set articleBedtime = entry.postDate|date_modify('+3 months') %}
{% set daysLeft = date(articleBedtime|date('c')).diff(now).days %}

{% if daysLeft > articleBedtime | date('c') %}
    Artikel nur Nichtmitglieder verfügbar. Bitte Loggen Sie sich ein.
{% else %}
    Artikel für Nichtmitglieder noch {{ daysLeft }} Tage verfügbar.
{% endif %}

