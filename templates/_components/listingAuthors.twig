{% import '_macros/macroAuthorField' as macro %}

{%- set authorQuery = entry.authors -%}
{% set esUrlDe = 'https://www.espacesuisse.ch/de/verband/organisation/geschaeftsstelle' %}
{% set esUrlFr = 'https://www.espacesuisse.ch/fr/association/organisation/secretariat' %}

<ul class="articleAuthors gridStart2 gridSpan10">
	{%- for item in authorQuery -%}
		<li class="authorItem">
			{%- if item.photo -%}

				{# Transform the Authors Portrait #}
    			{%- set portraitImage = craft.imagerx.transformImage(item.photo, 'authorPortrait')[0] -%}

				<img src="{{ portraitImage.url }}" alt="{{ item.name }}">
			{%- endif -%}
				<div>
					<p class="authorName">{{ item.name|replace({' ': '&nbsp;'})|raw }}</p>
					<p class="authorMeta">
						{%- if item.fPlainTextI18n2 or item.fPlainTextI18n3 -%}
							<span class="authorEducation">
								{{ macro.authorI18nField(item.fPlainTextI18n2, item.fPlainTextI18n3) }}.
							</span>
						{%- endif -%}
						{# {%- for cat in item.fTeam -%}
							{%- if cat.fBlankUrlString -%}
								<a class="authorTeam" href="{{ cat.fBlankUrlString }}">{{ cat.title ~ '.' }}</a>
							{%- else -%}
								<span class="authorTeam" >{{ cat.title ~ '.' }}</span>
							{%- endif -%}
						{%- endfor -%} #}
						{%- if item.fPlainTextI18n -%}
							<span class="authorFunction">
								{{ macro.authorI18nField(item.fPlainTextI18n, item.fPlainTextI18n4) }}
							</span>
						{%- endif -%}
						{% if item.isInGroup('espacesuisse') %}	
							{% if entry.language == 'de-CH' %}
								<a href="{{ esUrlDe }}">EspaceSuisse</a></span>
							{% else %}
								<a href="{{ esUrlFr }}">EspaceSuisse</a></span>
							{% endif %}
						{% endif %}
					</p>
				</div>
		</li>
	{%- endfor -%}
</ul>