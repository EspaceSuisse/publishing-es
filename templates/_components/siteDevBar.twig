{% if craft.app.env != 'production' %}
	<div class="devBar" style="position: fixed; top: 0; right: 0; background: #000; color: #fff; padding: 1em;">
		Env:
		{{ craft.app.env }}
		|
		Sprache: 
		{{ craft.app.language }} | 
		{% if entry.section.handle is defined %}
			Section:
			{{ entry.section.handle }}
		{% else %}
			Category:
			{{ category.group.handle ?? '?' }}
		{% endif %}
		|
		Current Site: {{ currentSite.handle }}
		{% if currentUser.username is defined %}
			User:
			{{ currentUser.username }}
		{% else %}
			Not logged in
		{% endif %}
	</div>
{% endif %}
